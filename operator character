1 a++ 是先对原来的值进行操作，后进行++操作
  ++a 是先对原来的值进行++操作，后进行操作
  这两个操作在函数运行过后，其值都改变，都+1
  Example
          b=a++-1           b=a-1;a=a+1
2 | 两边都会判定 
  || 如果一边已经为true，另一边就不会运算判定
3 switch（表达式） 表达式的值只能是byte,short,int,char,String,枚举
4 栈 用来存放数据地址 先进后出  堆 用来存放数据的内容
